# 功能设计梳理

## 核心意图

根据 Karpathy 的描述，我们完全可以替代语义相似度判断的 RAG，而使用 LLM 来进行检索本身的难题。
而且多 agent 天然隔离上下文污染，即隔离我们查找 wiki 相关文档的试错部分。

从而解决 RAG 本身带来的可能的一系列问题。

**WikiMind 的愿景**：把散乱的笔记，整理成一本**有目录结构、相互交叉引用的知识之书**。
不是传统 wiki（图谱可视化），也不是纯书籍（需要交叉引用关系网络）。
本质是 **llm-wiki-compiler 的书籍化**——章节目录 + 系统性讲述 + wikilink 双向引用。

### DDL背景

需要演示demo来参加以下的比赛, 自定义LLM wiki personal knowledge base赛道

### 核心交互原则

**不自动 agent update。必须用户显性要求 agent update wiki。**

## 功能设计

### 0. 宏观：凌乱笔记 → 个人 wiki 知识库

这是一个会长时间进行的大工程。这个迁移或者修改我们打算是让用户主动去点击"update wiki"来控制 agent 对 wiki 的迁移。

我们会根据用户的原始的非常杂乱的 md 笔记，让 agent 整理出一个 wiki 的笔记形式，然后供前端展示我们的 wiki 部分。

注意 wiki notes 和 my notes 是两个独立的文件。用户可以自行选择修改 wiki notes 本身，也可以修改 my notes 然后 update 为 wiki notes。

可以自定义每天或者一段时间定时触发 update wiki 操作（若产生了 my notes 更改）。

### 1. 初始新建 wiki

需要 spawn 多个 agent 来并发处理每一个文档的概要，然后最后根据整体笔记结构和用户偏好意图，创建出 wiki 的宏观结构，然后再 spawn 多 agent 来调查编写每一个 wiki 的内容，写完一版后主 agent 进行检查 + 最后的修复。

### 2. update wiki — user

用户可以直接修改 wiki notes 的具体内容，这个过程无需 agent 来干涉。

### 3. update wiki — agent

若交付给 agent 修改，agent 需能通过一些程序辅助表示判断出 my notes 哪些文件哪些行发生了修改，修改前是什么修改后是什么内容，来据此修改 wiki notes 的内容。这个标识我认为是很重要的。

---

## 更技术层面地设计功能实现

### 技术栈（已确定）

| 层级 | 技术 | 说明 |
|------|------|------|
| 桌面框架 | Tauri 2 | 打包 + 文件系统访问 |
| 前端 | React 18 + NaiveUI + Zustand | 前端同学负责 |
| 后端主体 | Rust（Tauri Commands） | 文件操作、进程管理 |
| AI 处理 | Python subprocess | Claude API 调用 |
| 存储 | 文件系统（Markdown） | 无数据库 |
| LLM | Claude API（用户提供 Key） | tool_use 模式 |

### 项目结构

```
exmind/
├── src/                         # Rust（Tauri Commands）
│   ├── main.rs                  # Tauri 入口
│   └── commands/
│       ├── mod.rs
│       ├── file_ops.rs          # list_dir / read_file / write_file
│       ├── chat.rs              # invoke("chat") → spawn python
│       └── diff.rs              # invoke("generate_diff") → spawn python
├── python/                       # AI 处理（Python subprocess）
│   ├── chat.py                  # 流式对话，输出 SSE 到 stdout
│   ├── diff.py                  # 生成 Diff
│   ├── llm.py                   # Anthropic SDK 封装
│   ├── extract_concepts.py       # Phase1（后续版本）
│   └── generate_wiki.py          # Phase2（后续版本）
├── src-tauri/
│   ├── Cargo.toml
│   └── tauri.conf.json
└── requirements.txt
```

### 后端 Tauri Commands（Rust 直接响应）

| 命令 | 签名 | 说明 |
|------|------|------|
| `list_dir` | `invoke("list_dir", {path}) -> Vec<FileInfo>` | 列出目录，区分文件和文件夹 |
| `read_file` | `invoke("read_file", {path}) -> string` | 读文件内容 |
| `write_file` | `invoke("write_file", {path, content})` | 写文件 |
| `create_file` | `invoke("create_file", {path, content})` | 新建文件 |
| `delete_file` | `invoke("delete_file", {path})` | 删除文件 |

### 后端 Tauri Commands（Python subprocess + SSE）

**Chat 流式对话：**
```
前端 → invoke("chat", {messages}) → Python spawn → Claude API streaming →
  SSE token → Tauri Event("chat-token") → 前端渲染
```

**Generate Diff：**
```
前端 → invoke("generate_diff", {original, instruction}) →
  Python spawn → Claude API → DiffResult {diffs: [{type, content}]}
```

### 前后端接口协议

```typescript
// ===== 文件操作（Rust 直接响应）=====
const files = await window.__TAURI__.core.invoke("list_dir", { path: "workspace/" });
const content = await window.__TAURI__.core.invoke("read_file", { path: "workspace/notes.md" });
await window.__TAURI__.core.invoke("write_file", { path: "workspace/notes.md", content: "..." });

// ===== Chat 流式对话 =====
// 前端订阅 token 事件
const unlisten = await window.__TAURI__.event.listen("chat-token", (event) => {
  const { token } = JSON.parse(event.payload);
  appendToMessage(token);
});
// 前端发送
await window.__TAURI__.core.invoke("chat", {
  messages: [{ role: "user", content: "帮我整理这段笔记..." }]
});

// ===== Generate Diff =====
const diff = await window.__TAURI__.core.invoke("generate_diff", {
  original: "原内容...",
  instruction: "把这段整理成 wiki 格式"
});
// diff.diffs = [{type: "add"|"remove"|"unchanged", content: "..."}]
// 前端渲染为 +绿色 -红色
```

### 知识流转（完整流程）

```
工作区（workspace/）
  ↓ 用户选中文件/内容
invoke("chat", {messages}) → 右栏 AI 协作面板（流式对话）
  ↓ 用户接受对话结果
invoke("generate_diff", {original: "", instruction: "..."})
  ↓ Diff 展示（+ - ）
  ↓ 用户 accept
invoke("write_file", {path: "agent-wiki/...", content}) → 写入 Agent Wiki
```

### DDL 倒计时任务分配

**今天（4.14）— 后端必须完成：**
- [ ] Rust 文件操作（Tauri Commands）
- [ ] Python chat.py（流式对话）
- [ ] Python diff.py（Diff 生成）
- [ ] 前端接入 chat + diff 能跑通

**明天（4.15）：**
- [ ] 三个面板联动（工作区选内容 → 右栏 chat → 审阅 diff → 写入 wiki）
- [ ] 整体 demo 流程跑通
- [ ] 比赛文档/PPT

**后天（4.16）：**
- [ ] 收尾 + 提交

---

## 技术栈选型

已确认为：
- **Tauri 2** + React 18（桌面打包 + 前端）
- **Rust** Tauri Commands（文件操作、进程管理）
- **Python subprocess**（AI 处理：chat、diff）
- **文件系统**存储（Markdown，无数据库）
- **Claude API**（Anthropic）
